#pragma once

/// <summary>
/// 글자 크기가 한 단위로만 다뤄지는지다: 선언은 논리 픽셀이고, 배율은 그리는 가장자리에서 한 번
/// 곱해진다.
///
/// 이 규칙이 깨진 자리는 화면에서 「글꼴이 좀 큰가」로만 보인다. 두 배가 된 글자가 그 요소의
/// 요구 크기를 두 배로 만들고, 그 크기로 잡힌 자리가 다시 어긋나므로, 증상은 글꼴이 아니라
/// 배치에서 나타난다 — 그래서 눈보다 이 검사가 먼저 잡아야 한다.
/// </summary>
[[nodiscard]] bool RunTextSizeUnitTests();
