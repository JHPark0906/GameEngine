#pragma once

/// <summary>
/// 버튼 바탕의 9-슬라이스가 배율에서도 제 크기로 잘리는지다.
///
/// 모서리는 늘어나면 안 되고, 가장자리는 한 방향으로만 늘어나며, 가운데가 나머지를 채운다. 이
/// 셋 중 하나라도 어긋나면 화면에서는 「9-슬라이스가 안 먹은 것 같다」로 보이는데, 그 인상만으로는
/// 어느 조각의 어느 값이 틀렸는지 알 수 없다 — 조각마다 크기를 재는 자리가 필요한 이유다.
/// </summary>
[[nodiscard]] bool RunSlicedButtonGeometryTests();
