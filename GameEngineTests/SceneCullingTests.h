#pragma once

/// <summary>
/// 직교 카메라 밖의 월드 공간 오브젝트가 실제로 걸러지는지 고정한다.
///
/// 「하나만 그려진다」는 것만 재면 컬링이 아무 일도 하지 않아도(늘 하나만 그린다면) 우연히
/// 맞을 수 있다. 그래서 심장은 카메라를 옮겨 <b>무엇이 남았는가</b>가 바뀌는 것을 보는
/// 것이다 — 수가 같아도 살아남은 것이 달라야 컬링이 실제로 답을 가르고 있다는 뜻이다.
///
/// 경계에 걸친 오브젝트는 그려야 한다: 반만 보이는 것을 지우는 것이 컬링의 가장 눈에 띄는
/// 실패이기 때문이다. 화면 공간은 카메라와 무관해야 하고, 원근 카메라는 오늘 절두체가 없어
/// 아무것도 걸러선 안 된다 — 틀린 상자로 거르면 오브젝트가 조용히 사라지는 버그가 되므로.
/// </summary>
[[nodiscard]] bool RunSceneCullingTests();
