#pragma once

/// <summary>
/// GPU 리소스를 쥔 캐시가 항목을 얼마나 오래 지켜야 하는지를 잰다.
///
/// 질문은 이것이다: 프레임 N의 command list가 아직 실행 중인데 프레임 N+1이 그 항목을 퇴거하면,
/// GPU가 읽고 있는 리소스를 해제하는 셈이 된다. 그래서 GPU 리소스를 쥔 캐시의 보존 창은 렌더러가
/// in flight로 유지하는 프레임 수에서 나와야 하고, <b>그 수를 베껴 적어서는 안 된다</b> — 베낀
/// 숫자는 원본이 바뀔 때 따라가지 않고, 따라가지 않았다는 사실은 눈에 보이지 않는다.
///
/// 그래서 두 가지를 못 박는다. 하나는 동작이다: 보존 창이 한 프레임이면 지난 프레임이 쓴 항목이
/// 바로 퇴거되고, in flight 프레임 수에서 나온 창이면 그 기간 동안 살아남는다. 다른 하나는
/// 규칙이다: D3D12에서 GPU 바인딩을 담는 캐시는 그 창을 <c>FramesInFlight</c>에서 유도한 값으로
/// 받아야 하며, <c>FrameSlotRetention</c> 자신도 그 관계로 정의되어 있어야 한다.
/// </summary>
[[nodiscard]] bool RunFrameRetentionTests();
