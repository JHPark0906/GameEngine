#pragma once

/// <summary>
/// 툴바 버튼 한가운데를 누르면 그 버튼이 답하는지다.
///
/// 클릭을 뷰에 직접 말하지 않고 이벤트 시스템으로 흘린다 — 커서를 옮기고, 누르고, 뗀다. 그래야
/// 「그려지는 자리에 커서가 닿는가」가 시험된다. 화면에서는 이 물음이 「버튼이 안 눌린다」로
/// 나타나고, 눌린 것이 엉뚱한 요소일 때는 무엇이 답했는지까지 알아야 원인을 짚을 수 있다.
/// </summary>
[[nodiscard]] bool RunToolbarClickRoutingTests();
