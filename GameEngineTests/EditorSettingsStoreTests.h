#pragma once

/// <summary>
/// 설정 저장소가 <b>바뀌었을 때만</b> 파일을 쓰는지 고정한다.
///
/// 그 규칙이 이름에 적혀 있다 — <c>SaveIfChanged</c> — 는 것이 이 자리를 두는 이유다. 즉시
/// 모드 UI는 매 프레임 같은 값을 다시 말하므로, 값이 같은데도 쓰면 디스크가 프레임마다 돈다.
/// 반대로 바뀌었는데 쓰지 않으면 사람이 배치한 것이 다음 실행에서 사라지고, 그러면 창을
/// 옮기는 기능 자체를 쓰지 않게 된다. 둘 다 화면에서는 보이지 않는 종류의 잘못이다.
///
/// 파일을 다시 썼는지는 수정 시각으로 잰다. 시각이 같은 초 안에서 구별되지 않는 문제가 있어,
/// 재기 전에 파일을 한 시간 뒤로 돌려 놓고 그 값이 그대로인지 본다.
/// </summary>
[[nodiscard]] bool RunEditorSettingsStoreTests();
