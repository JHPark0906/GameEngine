#pragma once

/// <summary>
/// 파일을 읽고 쓰는 두 규칙을 고정한다.
///
/// 읽기는 줄 끝을 건드리지 않는다 — 텍스트 모드는 Windows에서 그것을 바꿔 버려, 해시를 재거나
/// 바이트를 그대로 비교하는 자리에서 파일과 읽은 것이 달라진다.
///
/// 쓰기는 실패가 <b>파손</b>이 아니라 <b>무산</b>으로 끝나야 한다. 원자적 쓰기가 지키는 것이
/// 그것이고, 그래서 시험이 재는 것은 성공한 쓰기가 아니라 실패했을 때 목적지에 무엇이 남아
/// 있는가이다.
/// </summary>
[[nodiscard]] bool RunTextFileTests();
