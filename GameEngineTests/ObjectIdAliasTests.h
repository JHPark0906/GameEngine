#pragma once

/// <summary>
/// 커맨드가 잡아 둔 객체 id를 지금 살아 있는 id로 잇는 별칭 규칙을 고정한다.
///
/// 이 표가 있는 이유는 되돌리기가 객체를 지웠다 다시 만들면 그 객체가 <b>새 id</b>로 돌아오기
/// 때문이다. 커맨드는 대상을 id로 잡아 두므로, 이어 주지 않으면 그다음 되돌리기·다시하기가
/// 이미 없는 id를 가리키고 조용히 아무 일도 하지 않는다 — 오류도 로그도 없이 「눌렀는데
/// 아무 일도 안 일어남」으로만 보인다.
///
/// 재는 것은 셋이다. 사슬을 끝까지 따라가는가(한 객체가 여러 번 되살아나면 id가 여러 번
/// 바뀐다), 이미 별칭이 있는 id에 또 이을 때 사슬의 <b>끝</b>에 잇는가(중간에 이으면 먼저
/// 적힌 별칭이 끊긴다), 그리고 비울 때 이력과 표가 함께 비는가.
/// </summary>
[[nodiscard]] bool RunObjectIdAliasTests();
