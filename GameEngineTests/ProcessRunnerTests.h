#pragma once

/// <summary>
/// 프로그램 하나를 돌리고 그 출력을 읽는 설비다. 빌드·패키징·시험 실행이 같은 것을 쓰므로,
/// 여기서 고정하는 것은 그 셋이 함께 기대는 규칙이다: 줄이 순서대로 오는 것, 종료 코드가
/// 한 번 오는 것, 취소가 손자까지 끝내는 것, 그리고 콘솔이 쓴 글자가 깨지지 않는 것.
/// </summary>
[[nodiscard]] bool RunProcessRunnerTests();
