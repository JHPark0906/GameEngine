#pragma once

/// <summary>
/// 고정된 표 하나가 내는 매니페스트의 <b>바이트</b>를 그대로 못 박는다.
///
/// 이미 있는 결정성 시험(`RunAssetDatabaseTests`)은 「같은 표를 두 번 저장하면 두 파일이 같다」를
/// 본다. 그것은 한 실행 안의 성질이라, 저장하는 코드를 옮기거나 다시 쓰면서 형식이 함께 바뀌어도
/// 여전히 통과한다. 여기서 보는 것은 다른 질문이다: <b>어제의 바이트와 오늘의 바이트가 같은가</b>.
///
/// 매니페스트는 빌드가 읽는 파일이므로, 형식이 조용히 바뀌면 그것을 읽는 쪽이 먼저 알게 된다 —
/// 사람이 만든 패키지가 열리지 않는 자리에서. 그 사이에 있는 시험이 이것이다.
///
/// 기대값을 파일이 아니라 시험 안에 적어 두는 이유는, 바이트가 기계에 따라 흔들리지 않기
/// 때문이다: 매니페스트에는 절대 경로가 없고(상대 경로만), 줄바꿈은 코드가 직접 쓰며, 내용
/// 해시와 크기는 이 시험이 직접 쓴 파일 내용에서 나온다.
/// </summary>
[[nodiscard]] bool RunAssetManifestBytesTests();
