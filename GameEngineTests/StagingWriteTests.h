#pragma once

/// <summary>
/// 모든 스테이징이 임시 파일에 쓴 뒤 이름을 바꾸는 방식으로 목적지를 교체하는지 확인한다.
/// 빌드와 파일 읽기가 겹쳐도 읽는 쪽이 빈 파일이나 절반만 쓴 파일을 정상 결과로 받아서는 안 된다.
/// 이 규칙은 컴포넌트 스키마를 포함한 모든 스테이징 대상에 적용한다.
/// </summary>
[[nodiscard]] bool RunStagingWriteTests();
