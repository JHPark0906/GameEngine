#pragma once

/// <summary>
/// 타일맵이 카메라 밖의 칸까지 훑지 않는지 고정한다. 다른 부류의 컬링(sprite·mesh·text)은
/// 「그릴지 말지」를 오브젝트 하나마다 정하지만, 타일맵은 훑는 순서 자체가 격자 좌표라
/// 카메라 사각형을 격자 좌표로 한 번 바꿔 <b>범위 자체를 좁히는</b> 다른 메커니즘이다 — 그래서
/// 따로 잰다.
///
/// 재는 것은 draw에 실제로 실린 칸들의 (열, 행) 집합이다. 카메라가 격자의 일부만 덮으면 그
/// 부분의 칸만 실려야 하고, 넓히면 전부, 아예 벗어나면 draw 자체가 없어야 한다.
/// </summary>
[[nodiscard]] bool RunTilemapCullingTests();
