#pragma once

/// <summary>
/// 툴바 버튼이 자기 글자만큼 넓어지는지다.
///
/// 버튼을 <b>에디터가 쓰는 그 함수로</b> 세운다. 조립을 시험이 직접 하면, 크기를 재는 컴포넌트가
/// 글자 없는 오브젝트에 붙어 아무것도 재지 못해도 그 시험은 통과한다 — 시험이 세운 트리에는
/// 그 실수가 없기 때문이다. 조립을 시험이 하지 않는 것이 이 파일의 요점이다.
/// </summary>
[[nodiscard]] bool RunEditorToolbarButtonTests();
