#pragma once

// 애플리케이션 아이콘의 리소스 번호다. 창을 만드는 엔진과 아이콘을 싣는 애플리케이션이 이 번호
// 하나로 만난다: 엔진은 이 번호를 찾고, 애플리케이션은 자기 .rc에서 이 번호로 .ico를 선언한다.
//
// 헤더가 전처리기 정의뿐인 이유는 리소스 컴파일러가 이 파일을 함께 읽기 때문이다. C++ 선언이
// 들어오면 .rc가 그것을 파싱하지 못한다.
#define GAMEENGINE_APPLICATION_ICON 1
