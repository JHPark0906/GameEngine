#pragma once

namespace GameEngine::App
{

/// <summary>
/// 실행 중인 실행 파일과 함께 배포된 게임을 실행하고 프로세스 종료 코드를 반환한다.
///
/// 엔진이 플레이어 진입점을 제공한다. 게임 프로젝트는 콘텐츠와 선택적 C++ 컴포넌트를
/// 제공하며 main을 제공하지 않는다. 이 함수가 프로젝트 서술자를 찾고 로드해 Application을 실행한다.
///
/// 프로젝트 자신의 컴포넌트 타입은 정적 초기화에서 스스로 등록하므로,
/// 이 함수는 자기가 어느 프로젝트를 실행하는지 알 필요가 없다.
/// </summary>
[[nodiscard]] int RunPlayer();

}
